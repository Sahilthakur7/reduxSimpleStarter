// Create a new componenet. This compoenet should produce some HTML.
const App = function() {
    return <div>Bunty Baweja!</div>;
} 

// Take this component's generated HTML and put it on the page (in the DOM)
